# Create member specific routes:
    The routes include:
        registration -> making member a verified member
        authentication route
        bids categorization [types: 'daily', 'referral', 'creation']
        